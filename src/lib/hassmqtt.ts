export interface hassDevice {
    domain: string;
    entityID: string;
}

export function deleteDevice(id: string) {

}

export function addDevice(id: string, callback: (dev: hassDevice) => void) {

}